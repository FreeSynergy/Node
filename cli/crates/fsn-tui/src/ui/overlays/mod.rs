// Overlay renderers — floating popups drawn on top of the main layout.
//
// Each module renders one OverlayLayer variant.
// Overlays are drawn by ui/mod.rs after all layout slots are rendered.

pub mod welcome;
